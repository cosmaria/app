{
  "name": "core-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/core/domain/src",
  "projectType": "library",
  "tags": ["scope:core", "type:domain"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": { "cwd": "libs/core/domain", "command": "tsc --noEmit -p tsconfig.json" }
    },
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/dist"],
      "options": { "cwd": "libs/core/domain", "command": "tsc -p tsconfig.lib.json" }
    }
  }
}
