{
  "name": "comunidade-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/comunidade/domain/src",
  "projectType": "library",
  "tags": ["scope:comunidade", "type:domain"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "cwd": "libs/comunidade/domain",
        "command": "tsc --noEmit -p tsconfig.json"
      }
    }
  }
}
