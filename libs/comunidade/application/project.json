{
  "name": "comunidade-application",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/comunidade/application/src",
  "projectType": "library",
  "tags": ["scope:comunidade", "type:application"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "cwd": "libs/comunidade/application",
        "command": "tsc --noEmit -p tsconfig.json"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/dist"],
      "options": { "cwd": "libs/comunidade/application", "command": "tsc -p tsconfig.lib.json" }
    }
  }
}
